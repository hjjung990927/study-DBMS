1. 먼저 연결이 되어 있는 지 확인부터 한다.



--->>> 테이블을 만들고 ); 마지막 부분에 커서를 둔 후 새로고침(F5)를 해준다
--->>> 새로고침 후 다음 테이블에 한번 더 새로고침(F5) 해준 후 ERD(엔티티 관계도)를 확인해본다.

* unsigned ==> 부호 없는 정수형, 음수를 저장할 수 없는 대신 양수 범위를 두 배로 늘릴 수 있다.
* current_timestamp ==> 현재 시간 명령어

/*
 * 실행 순서
 * from -> join -> on -> where -> group by -> having -> select -> order by
 */